package org.zero.je4.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import org.zero.je4.entity.SpeechPart;

public interface SpeechPartRepository extends JpaRepository<SpeechPart, Integer> {
}
